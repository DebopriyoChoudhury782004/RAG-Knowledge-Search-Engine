# -----------------------------
# Local Free LLM + Embeddings
# -----------------------------
# Specify the HuggingFace embedding model to use
EMBEDDING_MODEL=<your-embedding-model-here>
# Specify the local LLM model for text generation
LOCAL_GEN_MODEL=<your-local-generation-model-here>

# -----------------------------
# FAISS index path (relative)
# -----------------------------
# Directory to store/load your FAISS vector index
FAISS_INDEX_DIR=<path-to-faiss-index>

# -----------------------------
# Ingest options
# -----------------------------
# Maximum number of tokens per document chunk
CHUNK_SIZE=<chunk-size-in-tokens>
# Number of overlapping tokens between chunks
CHUNK_OVERLAP=<chunk-overlap-in-tokens>

# -----------------------------
# API server
# -----------------------------
# Hostname or IP to run the API server
API_HOST=<api-host>
# Port number to run the API server
API_PORT=<api-port>
